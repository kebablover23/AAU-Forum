# Notification sound

The forum uses a short Web Audio notification tone from `public/js/app.js`, so no external audio dependency or autoplay-blocked media file is required. The tone is generated only after a real-time notification event.
